# 乐岛全局 UI 体检与迭代总纲

本文档用于指导乐岛后续 UI 迭代。它不是单次改版清单，也不是替代 `design.md` 的 token 规范，而是把当前界面的问题、目标方向、组件策略、页面优先级和验收标准固定下来，后续每一轮 UI 修改都应先对照本文档确定范围。

## 1. 体检结论

当前应用已经具备一套初步设计 token，也有若干可复用组件，例如 `AppContentPage`、`MusicTrackTile`、`MusicAlbumGridCard`、`MusicArtistGridCard`、`MetaPill` 和 `PlayAllButton`。问题不在于完全没有设计，而在于设计语言没有被足够稳定地贯彻到所有页面。

用户直观感受到的“不协调”主要来自五类设计债：

1. 组件词汇不统一  
   同样是按钮、搜索框、返回按钮、列表项、卡片、操作栏，不同页面经常重新实现一套尺寸、圆角、背景、hover、阴影和文案。局部能看，但组合起来不像同一个产品。

2. 情绪层级过度泛化  
   播放页、详情页可以有封面氛围、模糊和较强光影，但首页、搜索、媒体库、设置、下载也频繁出现渐变、毛玻璃、大圆角和阴影，导致工具型页面也显得过度装饰。

3. 字体系统削弱中文产品感  
   当前 `Righteous + Poppins` 带来一定品牌感，但对中文 UI、列表、按钮、设置项和桌面工具界面不够稳。标题和内容之间的气质差异有时过大，使应用看起来更像视觉实验，而不是成熟播放器。

4. PC 端和移动端的结构差异不够清楚  
   应用已经有 1080px 断点，但不少页面仍在用移动端思路处理桌面端，例如过多大卡片、过大间距、底部抽屉式操作、列表信息密度偏低。PC 端应该更像桌面播放器和媒体库管理工具。

5. 页面各自为政  
   搜索页、媒体库、首页、详情页、设置和下载页各自有一套局部布局逻辑。后续继续单页修补，会不断出现“这个按钮不协调”“这个 hover 奇怪”“这个区域太像卡片”的重复问题。

## 2. 产品气质目标

乐岛的整体方向应收敛为：

> 安静、封面驱动、偏原生工具感的自托管音乐播放器。

具体含义：

- 它首先是一个日常使用的播放器和媒体库浏览工具，不是营销页、后台系统或强视觉实验。
- 它应该让用户相信自己的音乐库被认真对待，界面可靠、稳定、有秩序。
- 音乐感主要来自封面、歌词、播放状态、细腻动效和低饱和氛围色，而不是大量装饰性渐变、毛玻璃和光影。
- PC 端强调浏览效率、信息密度和键鼠操作；移动端强调触控舒适、连续播放和沉浸。
- 深浅主题都必须成立。深色适合夜间聆听，浅色适合白天整理音乐库。

## 3. 全局设计原则

### 3.1 内容重于容器

封面、曲名、艺术家、专辑、歌词和播放状态是主角。容器只负责建立秩序，不应抢夺注意力。

落地要求：

- 列表页减少装饰性外框。
- 卡片只用于专辑、艺术家、歌单、设置分组等确实需要分组的对象。
- 歌曲列表优先使用行式结构，PC 端可采用表格化信息密度。

### 3.2 组件先于页面

任何页面级重做之前，先判断是否需要抽出或复用组件。

优先沉淀这些组件：

- `AppPageScaffold`
- `AppPageHeader`
- `AppSearchField`
- `AppScopeTabs`
- `AppActionButton`
- `AppIconButton`
- `AppTrackList`
- `AppTrackTable`
- `AppAlbumGrid`
- `AppArtistGrid`
- `AppStateView`
- `AppSheet`
- `AppDialog`

### 3.3 桌面端不是放大的移动端

PC 端应更克制、更密、更稳定。

落地要求：

- 桌面端减少全屏式大卡片。
- 搜索、媒体库、下载、设置应使用更强的信息结构和更轻的操作按钮。
- 底部抽屉主要服务移动端，桌面端优先使用 popover、inline panel 或右侧轻量面板。

### 3.4 播放页可以更有情绪，但控制必须清楚

播放页是乐岛最有音乐感的页面，可以使用封面模糊背景、歌词高亮、唱片动效和更强视觉焦点。

但必须满足：

- 播放、暂停、上一首、下一首、进度条、音量、队列始终清晰。
- 歌词和封面不能互相争夺主焦点。
- 移动端手势必须有可发现的替代入口。
- PC 端布局应像桌面播放器，不像移动页横向拉伸。

### 3.5 克制使用装饰性视觉

以下视觉手法只允许在明确场景使用：

| 手法 | 使用场景 | 禁止场景 |
|---|---|---|
| 封面模糊背景 | 播放页、专辑详情、艺术家详情 | 搜索、设置、下载、普通列表 |
| 毛玻璃 | MiniPlayer、播放页局部控制 | 页面 header、普通操作栏、每个卡片 |
| 强阴影 | 浅色模式下少量悬浮控件 | 深色列表项、密集卡片、普通按钮 |
| 大圆角 24px 以上 | 播放页、封面、shell 容器 | 普通列表项、输入框、工具按钮 |
| 渐变 | 封面氛围遮罩、登录页背景 | 文字、普通卡片、操作按钮 |

## 4. 设计系统重构方向

### 4.1 字体

建议目标：

- UI 主字体迁移到系统字体栈，优先保证中文和跨平台一致性。
- `Righteous` 仅保留在 Logo、极少数品牌性标题或播放页特殊展示中。
- 列表、按钮、标签、设置项、表格不使用 display 字体。

建议阶段：

1. 先修改 `AppTheme` 的正文、标题和标签字体策略。
2. 调整 `headline` 用法，避免普通页面标题过度品牌化。
3. 对播放页单独保留更强字体风格。

验收标准：

- 搜索、媒体库、设置、下载页看起来更接近成熟桌面应用。
- 中文按钮和列表项不再有明显的西文字体违和感。
- 标题层级清晰，但不抢内容。

### 4.2 色彩

现有主色方向可保留，但需要降低“到处强调”的频率。

调整方向：

- Primary 只用于主操作、当前播放、选中态、焦点态。
- Secondary 只用于辅助状态，不和 Primary 同时大面积出现。
- 浅色 `surface` 建议从纯白迁移到轻微染色的中性表面。
- 页面背景、容器背景、列表 hover、selected 需要统一透明度和层级。

验收标准：

- 页面不用依赖渐变也能成立。
- 列表 hover、选中、当前播放之间有明确区别。
- 深浅主题下信息层级一致。

### 4.3 圆角和层级

当前 20px、24px、28px、36px、999px 使用较多，容易导致所有元素都像“大胶囊”。

建议收敛：

| 角色 | 建议圆角 |
|---|---:|
| App shell 主容器 | 20-24 |
| 页面分组卡片 | 12-16 |
| 列表项 | 10-14 |
| 输入框 | 12-14 |
| 专辑封面 | 12-16 |
| 播放页大封面 | 20-24 |
| 胶囊主按钮 | 999 |
| 普通图标按钮 | 10-12 |

验收标准：

- 工具型页面不再充满大圆角和浮动卡片。
- 播放页和详情页仍然可以更柔和，但不会污染全局。

### 4.4 动效

现有动效方向基本合理，但需要统一节奏。

建议：

- hover、pressed、selected：150-180ms。
- 页面切换：尽量少做，避免每个页面进入都滑动。
- 歌词滚动：保留 320ms 左右。
- 播放页仪式感动画独立管理，不能影响控制响应。

验收标准：

- 列表滚动时没有多余动效负担。
- hover 和选中态反馈统一。
- 播放页有情绪，但不拖慢操作。

## 5. 核心组件改造清单

### 5.1 页面壳层

相关文件：

- `lib/presentation/widgets/app_shell.dart`
- `lib/presentation/widgets/layout/page_layout.dart`
- `lib/presentation/widgets/mini_player_bar.dart`

问题：

- 桌面 sidebar、主内容容器、MiniPlayer 都有较强边框、阴影、圆角和透明层，整体层级偏重。
- 主内容区像一个大浮层，部分页面内部又继续使用大量卡片，形成容器套容器。
- MiniPlayer 的视觉比一些主页面内容还强，容易抢焦点。

方向：

- Shell 保留稳定边界，但降低阴影和玻璃感。
- MiniPlayer 作为常驻控制条，应更精密、更薄、更像播放器控件。
- 桌面 sidebar 使用更明确的导航状态，不依赖过多容器装饰。

验收标准：

- 打开任意页面时，用户首先看到内容，不是壳层装饰。
- MiniPlayer 不遮蔽页面节奏，但当前播放足够清晰。

### 5.2 页面 Header

相关文件：

- `lib/presentation/widgets/layout/page_layout.dart`
- `lib/presentation/pages/search/search_page.dart`
- `lib/presentation/pages/library/library_page.dart`
- `lib/presentation/pages/downloads/downloads_page.dart`

问题：

- Header 在不同页面的结构不统一，返回按钮、标题、描述、搜索框、统计 pill、操作按钮位置经常变化。
- PC 端搜索页和普通页面 header 的视觉规则不完全一致。

方向：

- 抽象统一 `AppPageHeader`，支持返回、标题、描述、主操作、次操作、搜索槽位、统计槽位。
- PC 端 header 使用同一基线和高度规则。
- 移动端 header 优先保留返回和主任务，不挤入过多信息。

验收标准：

- 返回按钮、标题、搜索框和操作按钮在所有页面位置一致。
- Header 不再需要每个页面手写 Row。

### 5.3 搜索框

相关文件：

- `lib/presentation/pages/search/search_page.dart`
- `lib/presentation/pages/library/library_page.dart`

问题：

- 搜索框大小、suffixIcon、focus 阴影、dense 模式容易和页面其它控件不协调。
- 搜索页和媒体库搜索不是同一组件。

方向：

- 抽出 `AppSearchField`。
- 固定 PC 端高度、图标尺寸、清空按钮样式、focus border。
- 清空按钮使用极简图标，不使用额外圆底。
- 搜索提交、输入中、清空、禁用状态统一。

验收标准：

- 搜索页、媒体库、歌单搜索复用同一视觉语言。
- PC 端输入框高度和附近按钮协调。

### 5.4 按钮系统

相关文件：

- `lib/shared/theme/app_theme.dart`
- `lib/presentation/widgets/music/play_all_button.dart`
- `lib/presentation/pages/search/search_page.dart`
- `lib/presentation/pages/player/player_page.dart`

建议建立四种按钮角色：

| 角色 | 用法 | 视觉 |
|---|---|---|
| Primary | 播放全部、登录、确认主操作 | Filled，强调色 |
| Secondary | 加入队列、下载、返回媒体库 | Tonal 或 Outlined |
| Ghost | 清空、取消、轻量文本操作 | 无背景，仅 hover |
| Icon | 返回、关闭、播放控制、更多 | 固定尺寸，极简背景 |

验收标准：

- 同一个操作角色在所有页面视觉一致。
- PC 端批量操作按钮不再像移动端大按钮。
- destructive 操作有明确但克制的危险态。

### 5.5 列表与表格

相关文件：

- `lib/presentation/widgets/music/music_track_tile.dart`
- `lib/presentation/pages/library/library_page.dart`
- `lib/presentation/pages/search/search_page.dart`
- `lib/presentation/pages/downloads/downloads_page.dart`
- `lib/presentation/pages/history/history_page.dart`
- `lib/presentation/pages/favorites/favorites_page.dart`

问题：

- 歌曲列表有 `card`、`row`、`list` 多种形式，但页面仍会自定义表格行和下载行。
- PC 端歌曲列表需要更像表格，移动端需要更像触控列表。
- hover、current、selected、pressed 状态需要统一。

方向：

- 抽出 `AppTrackList` 和 `AppTrackTable`。
- PC 端表格列建议：序号、封面、歌曲、艺术家、专辑、时长、操作。
- 移动端列表建议：封面、标题、副标题、状态图标、更多。
- 当前播放状态统一使用 `primaryContainer` 背景、细边框、播放中图标。

验收标准：

- 搜索歌曲、媒体库歌曲、专辑歌曲、歌单歌曲、历史、收藏使用同一状态语言。
- hover 当前项不会影响相邻项。
- 列表密度在 PC 端明显提升。

### 5.6 专辑、艺术家、歌单卡片

相关文件：

- `lib/presentation/widgets/music/music_album_cards.dart`
- `lib/presentation/widgets/music/music_artist_card.dart`
- `lib/presentation/pages/playlists/playlists_page.dart`

问题：

- 专辑卡片、紧凑专辑卡片、艺术家卡片、歌单卡片视觉关系不够清晰。
- 有些卡片是封面主导，有些是容器主导，整体不稳定。

方向：

- 专辑和歌单以封面为主，文字为辅。
- 艺术家以头像为主，卡片容器更轻。
- hover 只做轻微 scale 或背景变化，不叠加强阴影。

验收标准：

- 搜索、媒体库、艺术家详情中的专辑卡片一致。
- 歌单卡片和专辑卡片有亲缘关系，但能通过图标或 meta 区分类型。

### 5.7 空状态、加载、错误

相关文件：

- `lib/presentation/widgets/layout/page_layout.dart`
- 各页面的空状态调用处

问题：

- 当前空状态多为一句话，缺少图标、下一步操作和页面语境。
- 加载状态常用居中 spinner，内容区域较空。

方向：

- `AppStateView` 支持 icon、title、description、primaryAction、secondaryAction。
- 列表加载使用 skeleton 或局部占位，减少大面积 spinner。
- 错误状态给出重试操作和简短原因。

验收标准：

- 用户知道为什么为空，以及下一步能做什么。
- 加载和错误状态不破坏页面结构。

## 6. 页面级改造方向

### 6.1 App Shell

目标：建立全应用稳定骨架。

优先事项：

- 降低桌面主容器、sidebar、MiniPlayer 的装饰强度。
- 统一桌面和移动底部区域的安全间距。
- 调整 sidebar 导航按钮尺寸、选中态和 tooltip。

验收标准：

- 壳层安静，内容优先。
- macOS 红绿灯区域、返回按钮和内容边距统一。
- MiniPlayer 像常驻播放器控件，而不是浮动营销卡片。

### 6.2 搜索页

目标：成为 PC 端最清晰、最高频的查找入口。

优先事项：

- Header、搜索框、返回按钮、统计信息统一。
- 分类直接使用歌曲、专辑、艺术家、歌单，不显示“全部”。
- 歌曲 tab 用 PC 表格或高密度列表，专辑/艺术家/歌单用统一网格。
- 批量操作使用极简按钮。

验收标准：

- PC 端搜索框比例协调。
- tab 文案居中，状态清楚。
- 列表 hover、当前播放、操作按钮统一且没有相邻项污染。

### 6.3 媒体库

目标：成为整理和浏览音乐库的核心工具页。

优先事项：

- 与搜索页共享 `AppSearchField`、tabs、歌曲表格、专辑/艺术家网格。
- PC 端歌曲列表提高信息密度。
- 移动端保持卡片式触控列表，但减少过厚容器。

验收标准：

- 搜索页和媒体库像同一个系统的两个入口。
- 歌曲、专辑、艺术家、收藏四个 tab 的内容区规则稳定。

### 6.4 首页

目标：快速开始聆听，而不是视觉展示页。

优先事项：

- 搜索入口降低玻璃感，和全局搜索框语言一致。
- 今日推荐、最近播放、常听、最近加入建立统一区块节奏。
- 减少首页中每个区块都像特殊组件的感觉。

验收标准：

- 首页首屏能快速进入搜索、推荐或最近播放。
- 视觉有音乐感，但不抢媒体库和播放页的主功能。

### 6.5 播放页

目标：保留情绪强度，但让布局更稳定、更成熟。

优先事项：

- PC 端重新定义封面、歌词、控制区三栏或两栏关系。
- 移动端明确封面/歌词切换入口，减少隐藏手势依赖。
- 工具按钮统一为播放页专用 icon button。
- 歌词区域保持稳定居中，不被装饰卡片压迫。

当前落地：

- PC 端播放页已重排为封面舞台、歌词舞台、右侧播放工具面板和底部紧凑控制条。
- 当前曲目信息和收藏动作放回封面舞台，底部控制条只保留进度、核心播放控制和当前音质。
- 右侧播放工具面板统一下载、睡眠定时、队列、音质和音量入口，减少桌面端移动式抽屉依赖。
- 移动端新增显式「封面 / 歌词」分段切换，封面点击不再承担隐藏模式切换。
- 歌词舞台去掉内部嵌套卡片，歌词内容直接在稳定区域中居中滚动。

验收标准：

- 播放页是全应用最有情绪的页面，但控制没有歧义。
- PC 端不是移动布局横向拉伸。
- 歌词、封面和控制区的视觉焦点明确。

### 6.6 详情页

范围：

- 专辑详情
- 艺术家详情
- 歌单详情

目标：统一身份展示和曲目列表。

优先事项：

- 统一 Detail Hero：封面/头像、标题、meta、主操作。
- 桌面端 hero 更横向、更高密度。
- 曲目列表复用 `AppTrackList` 或 `AppTrackTable`。

验收标准：

- 专辑、艺术家、歌单详情有一致结构。
- 封面氛围存在，但不压低文字可读性。

### 6.7 设置与下载

目标：回到安静、清楚、可信的工具界面。

优先事项：

- 设置分组卡片降低圆角和阴影。
- 下载行复用列表组件，而不是独立视觉。
- 底部 sheet 与桌面操作方式区分。

验收标准：

- 设置像产品配置页，不像卡片展示页。
- 下载管理信息密度更高，状态和操作明确。

## 7. 多端布局策略

### 7.1 PC 端，>=1080px

设计关键词：稳定、密度、键鼠效率。

布局要求：

- 左侧导航固定。
- 主内容区使用统一 header。
- 歌曲类内容优先表格或高密度列表。
- 专辑、艺术家、歌单使用响应式网格。
- 批量操作在内容上方 inline 展示。
- 简单操作不默认使用底部抽屉。

### 7.2 中宽屏，600px 到 1079px

设计关键词：内容优先、适度收敛。

布局要求：

- 底部导航保留。
- 网格列数减少。
- 表格退化为列表。
- Header 中的描述和统计可以隐藏。

### 7.3 手机，<600px

设计关键词：触控、连续播放、单手可达。

布局要求：

- 底部导航和 MiniPlayer 组成稳定底部区域。
- 列表项高度不低于 56px，关键触控目标不低于 44px。
- 搜索、播放、队列、下载等高频操作优先靠近底部或内容主路径。
- 页面标题和操作避免横向拥挤。

## 8. 分阶段实施路线

### Phase 0：建立 UI 基线

目标：让后续每次改动都有比较标准。

任务：

- 为主要页面截取深浅主题、PC 和移动截图。
- 建立页面清单：登录、首页、搜索、媒体库、歌单、详情、播放、设置、下载。
- 记录当前组件使用情况和重复实现位置。

建议验证：

- `flutter analyze`
- 相关 widget test
- 人工截图对比

### Phase 1：全局 token 和字体收口

目标：先让所有页面的基础气质变稳。

任务：

- 调整 `AppTheme` 字体策略。
- 收敛圆角、阴影、surface 透明度。
- 统一按钮、输入框、IconButton 的 theme 默认值。
- 更新 `design.md` 中对应 token。

涉及文件：

- `lib/shared/theme/app_theme.dart`
- `lib/shared/theme/app_tokens.dart`
- `design.md`

验收：

- 页面整体更像产品 UI。
- 中文排版更自然。
- 普通页面不再过度品牌化。

### Phase 2：基础组件抽象

目标：停止页面各自发明 UI。

任务：

- 新增或重构 `AppPageHeader`。
- 新增 `AppSearchField`。
- 新增统一按钮封装或按钮样式 helpers。
- 扩展 `AppStateView`。
- 定义列表和表格组件接口。

涉及文件：

- `lib/presentation/widgets/layout/page_layout.dart`
- `lib/presentation/widgets/music/`
- 可新增 `lib/presentation/widgets/controls/`

验收：

- 搜索、媒体库、下载、设置开始复用同一套 header 和控件。
- 页面代码减少重复 Row、IconButton style 和局部 DecoratedBox。

### Phase 3：搜索页和媒体库重做

目标：优先处理最高频的浏览和查找体验。

任务：

- 搜索页复用 `AppPageHeader` 和 `AppSearchField`。
- 媒体库复用相同搜索和 tab。
- PC 端歌曲列表改为统一表格或高密度列表。
- 专辑、艺术家、歌单网格统一。

验收：

- 搜索页和媒体库的结构、控件、状态一致。
- PC 端视觉明显更成熟。
- 移动端不牺牲触控体验。

### Phase 4：首页、歌单和详情页统一

目标：统一内容消费路径。

任务：

- 首页区块节奏收敛。
- 歌单列表和专辑/艺术家网格建立一致卡片语言。
- 专辑、艺术家、歌单详情统一 hero 和曲目列表。

验收：

- 从首页进入详情、再进入播放，视觉路径连续。
- 详情页不再各自拥有完全不同的 hero 结构。

当前落地：

- 首页搜索入口已降低玻璃感，今日推荐和最近加入复用专辑网格语言，最近播放 hover 改为低透明遮罩。
- 歌单列表已迁移到 `MusicPlaylistGridCard` / `MusicPlaylistListTile`，与专辑卡片形成同一套封面主导语言。
- 专辑、艺术家、歌单详情已统一到 `AppDetailHeroFrame`；PC 端曲目列表复用 `MusicTrackTable`，移动端保留 `MusicTrackTile`。
- 详情页主操作收敛为 `PlayAllButton`，次操作收敛为 `AppActionButton`。

### Phase 5：播放页专项重构

目标：把播放页做成乐岛的情绪核心。

任务：

- PC 端重新规划布局。
- 移动端封面/歌词模式可发现化。
- 统一播放页工具按钮和控制区。
- 保留封面氛围，但降低控制区复杂度。

当前落地：

- `player_page.dart` 已形成 PC 三列工作区：封面、歌词、播放工具，底部为紧凑播放控制条。
- PC 工具动作收敛为 `_DesktopUtilityTile` 和内联音量控制，睡眠定时和音质使用桌面轻量弹层。
- 移动端增加「封面 / 歌词」显式分段切换，保留滑动作为辅助操作。
- 普通弹层从玻璃渐变收敛为实底 surface、轻边框和低阴影，唱片区域继续保留播放页专属氛围视觉。

验收：

- 播放页有明显高于普通页面的沉浸感。
- 控制响应清楚，歌词可读，封面不压迫信息。
- 深浅主题都成立。

### Phase 6：设置、下载、底部 sheet 和弹窗

目标：补齐工具界面的可信度。

任务：

- 设置项、下载项统一列表视觉。
- 桌面端减少移动式 bottom sheet。
- Dialog、Sheet、Popover 建立统一规范。

当前落地：

- 新增 `AppSheetScaffold`、`AppOptionTile`、`showAppConfirmationDialog`，统一移动端 Sheet、选择项和确认弹窗的视觉语言。
- 设置页的外观、播放、媒体来源、存储、账户分组收敛为轻量 surface 列表，减少默认 Card 的浮动感。
- 设置页默认音质和曲间间隔使用统一 Sheet 选择项，不再手写移动式列表。
- 退出登录、清理缓存、删除下载、清空播放队列接入确认弹窗，危险操作有明确后果说明。
- 播放音质、睡眠定时、播放队列、曲目操作 Sheet 已接入统一容器，去掉各自手写的圆角、拖拽把手和装饰渐变。

验收：

- 设置和下载页更安静、更高效。
- 删除、退出、清理等高风险操作反馈清楚。

### Phase 7：可访问性和最终 polish

目标：让 UI 可长期维护。

任务：

- 检查 tooltip、Semantics、键盘焦点。
- 检查大字体缩放。
- 检查深浅主题对比度。
- 补充关键 widget tests。
- 更新 `design.md` 和本文档中的完成状态。

当前落地：

- MiniPlayer 展开入口语义改为单击，并补充当前曲目和艺术家信息。
- MiniPlayer 与播放页音量、播放进度 Slider 补充读屏格式，音量读百分比，进度读时间。
- `MusicTrackTable` 行内操作视觉保持极简，但表头、行尾容器和图标按钮统一为 44px 命中区。
- 收藏按钮在桌面紧凑表格里不再收缩实际点击区域，底部导航补充 selected 语义。
- 补充 `MusicTrackTable` 行内操作 tooltip 与 44px 触控目标回归测试。
- `design.md` 明确紧凑图标视觉和 44px 实际命中区、Slider 语义格式规则。

验收：

- 常用图标操作都有可访问名称。
- 触控目标满足 44px。
- 关键页面无文本溢出和明显布局跳动。

### Phase 8：全局验收与缺陷清零

目标：把 Phase 1-7 的全局 UI 改造做一次跨页面收口，优先清理会影响真实使用的交互漂移和可访问性漏点。

任务：

- 复扫首页、搜索、媒体库、详情页、播放页、设置、下载、历史、收藏、队列和通用弹层。
- 检查通用按钮、返回、拖拽、Sheet 操作项是否满足 44px 触控目标。
- 检查剩余 `ListTile`、`shrinkWrap`、自定义点击区是否造成视觉或语义漂移。
- 补充公共组件回归测试，避免后续 UI polish 把触控目标压小。
- 运行格式化、目标测试和全量 `dart analyze lib test`。

当前落地：

- `AppActionButton` dense 模式实际高度提升到 44px，保持轻量视觉但不再收缩命中区。
- `AppBackButton` 从 40px 调整为 44px，返回入口与全局触控底线一致。
- 播放页桌面队列和 `QueueSheet` 的拖拽把手统一为 44px，并补充 Semantics。
- `AppOptionTile` 增加最小高度约束，移动端 Sheet 选项统一保持足够触控区。
- 曲目操作 Sheet 从原生 `ListTile` 迁移到 `AppOptionTile`，对齐音质、睡眠定时、队列等弹层的视觉和交互语言。
- 补充 `AppActionButton` 和 `AppBackButton` 的 44px 触控目标回归测试。
- 播放页移动端「更多操作」从手写底部 Sheet 迁移到 `AppSheetScaffold` 和 `AppOptionTile`。
- `AppOptionTile` 支持 disabled 状态，禁用项不触发操作，视觉和语义同步弱化。
- 补充 `AppOptionTile` 高度和 disabled 行为回归测试。
- 补充搜索页移动端和桌面端 responsive smoke 测试，检查核心结构渲染时无布局异常。
- 实际启动 macOS 桌面端完成首屏检查：应用可正常构建启动，登录页在桌面窗口下无明显布局崩坏；已登录页面仍需依赖可用会话或后续专用 fixture 做截图验收。
- `AppOptionTile` 的 hover、focus、pressed 收敛为单一外层状态源，关闭内部 Ink overlay，减少 Sheet 选项叠色和相邻项残留高亮的概率。
- 新增 `global_ui_smoke_test.dart`，覆盖设置页、下载页、队列 Sheet 在 390×844 与 1280×900 下的布局 smoke，避免后续全局 UI 改动遗漏工具页和弹层。
- 复扫剩余 `ListTile`、`InkWell`、`MouseRegion` 和小尺寸点击区：操作 Sheet 未回退到原生 `ListTile`；搜索/媒体库窄屏 `ListTile` 仅保留为普通内容列表语义。
- `AppScopeTabs` 的 hover、focus、pressed 同步收敛为单一外层状态源，避免 Tab 内部 Ink overlay 与自定义选中态叠加。
- 当前 Codex 运行环境无法通过系统 `screencapture` 或 `flutter screenshot` 获取 macOS 应用截图；真实应用已能启动并加载，截图级验收需在本机手动截图或后续补专用 golden fixture。

验收：

- P0/P1 级可访问性和触控目标问题已清零。
- 仍保留的 `ListTile` 只用于窄屏普通内容列表或原生列表语义，不用于通用操作 Sheet。
- 目标测试和全量静态分析通过。
- 当前环境的 widget test 执行依赖 Flutter SDK cache 写入或 sqlite3 预编译库下载；若权限或网络不可用，至少保留全量静态分析和 smoke 测试代码覆盖。

## 9. 每次 UI 迭代的执行模板

每次开始 UI 任务前，按以下模板确定范围：

```md
## 本轮目标

- 页面/组件：
- 要解决的问题：
- 不处理的范围：

## 设计依据

- 对应本文档章节：
- 对应 design.md token：

## 改动清单

- 文件：
- 新增组件：
- 替换旧实现：

## 验收标准

- PC：
- 移动：
- 深色：
- 浅色：
- 可访问性：

## 验证

- flutter analyze
- 相关 flutter test
- 截图检查
```

## 10. 全局 UI 验收清单

每次 UI 改动完成后至少检查：

- 当前页面是否复用已有组件，而不是重新手写类似控件。
- PC 和移动端是否使用不同结构，而不是简单缩放。
- 深色和浅色主题是否都可读。
- hover、pressed、selected、disabled 是否清楚。
- 图标按钮是否有 tooltip 或 Semantics。
- 触控目标是否不小于 44px。
- 标题、按钮、列表项文本是否会溢出。
- 是否引入了不必要的渐变、毛玻璃、大阴影或大圆角。
- 当前改动是否让应用更像一个统一产品，而不是只让单页更热闹。

## 11. 明确禁止的回退方向

后续 UI 迭代避免以下方向：

- 把每个页面都做成大卡片堆叠。
- 用更多渐变、模糊和阴影掩盖结构问题。
- 为普通按钮添加强背景和过大圆角。
- 在 PC 端照搬移动端底部抽屉和大间距。
- 让播放页之外的工具页承担过多“沉浸感”。
- 为了品牌感牺牲中文可读性。
- 只修单个按钮，不同步沉淀组件规则。

## 12. 推荐优先级

如果后续只能按顺序做，建议优先级如下：

1. Phase 1：字体、圆角、阴影、按钮和输入框 token 收口。
2. Phase 2：`AppPageHeader`、`AppSearchField`、按钮和状态视图组件化。
3. Phase 3：搜索页和媒体库重做。
4. Phase 5：播放页专项重构。
5. Phase 4：首页、歌单和详情页统一。
6. Phase 6：设置、下载、Sheet 和 Dialog 收口。
7. Phase 7：可访问性、测试和最终 polish。

这套顺序的原因是：先修全局语言，再修最高频页面，最后打磨强情绪页面和工具页面。如果先继续单页修补，短期能改善截图，但长期仍会反复出现不协调。
